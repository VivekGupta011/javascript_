// array is object that can store multiple value at once
// we can create array using two ways
// 1.using an array literal
const array1 = ["hello", "vivek"];

// 2.using the new keyword
const array2 = new Array("eat", "sleep");

// Empty array
const mylist = [];

// array of strings
const stringarray = ["hello", "vivek", "newth"];

//array of number
const numberarray = [1, 3, 4, 5, 6];

//array with mixed data types
var newdata = ["work", "exercise", 1, true];

// you can also store array,functions and other objects inside array
var newdata = [
  { task1: "exercise" },
  [1, 2, 3],
  function hello() {
    console.log("hello vivek");
  },
];

