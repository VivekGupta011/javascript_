const namee='vivek';
const name1="gupta";
const result=`the names are ${namee} and ${name1}`;
console.log(result);
//backtricks are generally used when you need to include variable or expressions into a string.
// this done by wrapping variable or expression ${variable or expression}